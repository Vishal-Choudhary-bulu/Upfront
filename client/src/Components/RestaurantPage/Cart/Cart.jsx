import React, { Component } from 'react'
import './Cart.css'

export default class Cart extends Component {
    render() {
        return (
            <div className="Cart">

                {/* Note: All the child elements should have a className of the format "Cart-yourDecidedName" */}

                {/* holds all the dishes a user has added to the cart and prices */}

                {/* appers inside the restaurant Home Component */}
                
            </div>
        )
    }
}
